#include <iostream>
#include<cstring>
#include "String.h"

int main()
{
    //CustomString str = CustomString("Hello, ", 8, 6);
    //CustomString str2 = CustomString("World!", 8, 5);
    //CustomString concatedStr;
    //append(str, str2).toString();

    //concatedStr.toString();
    //str1 = str2;
    //str1.pushBack('5');
    //std::cout << str1.getValue() << std::endl;
    
    /*char* s = new char [strlen(str.getValue()) + strlen(str2.getValue()) + 1];
    strcpy_s(s, strlen(str.getValue()) + strlen(str2.getValue()) + 1, str.getValue());
    strcat_s(s, strlen(str.getValue()) + strlen(str2.getValue()) + 1, str2.getValue());
    std::cout << s;*/

    return 0;
}